@{
    Script.Include("admin.edit-widget.js", "admin.edit-widget.min.js");
    Layout.Title = T("Add Widget").ToString();
}

@using (Html.BeginFormAntiForgeryPost()) {
    @Html.ValidationSummary()
    @Html.Hidden("ContentId", (int)Model.ContentId)
    @Display(Model)
}